# Frontend Client

This is a minimal React interface using framer-motion from CDN. It provides pages for login, registration, and password recovery. After successful login the user is redirected to a simple welcome screen.

Open `index.html` in a web browser. The backend server must run on `http://localhost:3001` for API calls.
